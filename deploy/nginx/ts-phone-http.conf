# Install once inside the nginx http context, normally under /etc/nginx/conf.d/.
limit_req_zone $binary_remote_addr zone=ts_phone_api:10m rate=10r/s;
limit_conn_zone $binary_remote_addr zone=ts_phone_connections:10m;
